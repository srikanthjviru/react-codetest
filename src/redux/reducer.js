import {
  GET_IMAGES,
  GET_IMAGES_SUCCESS,
  GET_IMAGES_FAIL,
  SET_SELECTED_IMAGE,
  CLEAR_SELECTED_IMAGE
} from "./types";

const reducer = () => {
  // TODO: Create reducer logic
};

export default reducer;
